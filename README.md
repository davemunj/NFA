The project implements a non-deterministic finite-state automaton (NFA). 
The NFA is specified via a text file, which speicifes the different states, whether they are accept or reject states, the list on null transitions between states and the list of input driven transitions.
